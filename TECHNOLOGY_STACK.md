# Технологічний стек та архітектура проекту

## 🎯 Обґрунтування вибору технологій

### Backend: Node.js + Express.js + TypeScript

**Чому Node.js?**
- ✅ Швидкий розвиток
- ✅ Одна мова для frontend та backend (JavaScript/TypeScript)
- ✅ Велика екосистема пакетів (npm)
- ✅ Асинхронна природа - добре для I/O операцій
- ✅ Легко знайти розробників
- ✅ Хороша підтримка REST API

**Альтернативи:**
- **Python + FastAPI**: Швидкий, сучасний, але менш популярний для веб-додатків
- **Java + Spring Boot**: Надійний, але більш важкий для навчального проекту
- **C# + ASP.NET Core**: Хороший вибір, але потребує .NET середовища

### Frontend: React.js + TypeScript

**Чому React?**
- ✅ Найпопулярніший фреймворк
- ✅ Велика спільнота та ресурси
- ✅ Багато готових компонентів (Material-UI, Ant Design)
- ✅ Гнучкий та масштабований
- ✅ Добре підходить для адмін-панелей

**Альтернативи:**
- **Vue.js**: Простіший для початківців, менша екосистема
- **Angular**: Більш структурований, але важчий для навчального проекту

### База даних: PostgreSQL

**Чому PostgreSQL?**
- ✅ Надійна реляційна БД
- ✅ Підтримка складних запитів (важливо для звітів)
- ✅ Безкоштовна та open-source
- ✅ Добре підтримується TypeORM
- ✅ Підтримка транзакцій

**Альтернативи:**
- **MySQL**: Схожа, але менш потужна для складних запитів
- **MongoDB**: NoSQL, але для цього проекту реляційна БД краща

### ORM: TypeORM

**Чому TypeORM?**
- ✅ Підтримка TypeScript
- ✅ Активна розробка
- ✅ Підтримка міграцій
- ✅ Добре працює з PostgreSQL
- ✅ Підтримка Repository pattern (вбудована)

**Альтернативи:**
- **Prisma**: Сучасніший, але менш гнучкий
- **Sequelize**: Популярний, але менш TypeScript-friendly

---

## 🏗 Архітектура системи

### Загальна архітектура

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (React)                      │
│  ┌──────────────┐              ┌──────────────┐         │
│  │ Client UI    │              │ Admin Panel  │         │
│  └──────────────┘              └──────────────┘         │
└─────────────────────────────────────────────────────────┘
                          │
                          │ HTTP/REST
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Backend (Node.js + Express)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ Routes   │→ │Controllers│→ │ Services │             │
│  └──────────┘  └──────────┘  └──────────┘             │
│                                    │                     │
│  ┌─────────────────────────────────┘                     │
│  │                                                       │
│  ▼                                                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │Repository│→ │  Models  │→ │ Patterns │             │
│  └──────────┘  └──────────┘  └──────────┘             │
└─────────────────────────────────────────────────────────┘
                          │
                          │ TypeORM
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Database (PostgreSQL)                       │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                │
│  │ Cars │  │Clients│ │Rentals│ │Penalties│             │
│  └──────┘  └──────┘  └──────┘  └──────┘                │
└─────────────────────────────────────────────────────────┘
```

### Шарова архітектура (Layered Architecture)

```
┌─────────────────────────────────────┐
│      Presentation Layer              │  ← React Components
│  (Controllers, Routes, Middleware)   │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│      Business Logic Layer            │  ← Services, Patterns
│  (Services, Domain Logic)            │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│      Data Access Layer               │  ← Repositories, ORM
│  (Repositories, Models)              │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│      Database Layer                  │  ← PostgreSQL
│  (Tables, Relations)                 │
└─────────────────────────────────────┘
```

---

## 🔧 Детальний опис компонентів

### Backend структура

#### 1. Config (Singleton Pattern)
```typescript
// config/database.config.ts
class DatabaseConfig {
  private static instance: DatabaseConfig;
  // Конфігурація підключення до БД
}
```

#### 2. Models (TypeORM Entities)
```typescript
// models/Car.entity.ts
@Entity()
class Car {
  @PrimaryGeneratedColumn()
  id: number;
  
  @Column()
  brand: string;
  
  @Column()
  model: string;
  
  @Column()
  year: number;
  
  @OneToMany(() => Rental, rental => rental.car)
  rentals: Rental[];
}
```

#### 3. Repositories (Repository Pattern)
```typescript
// repositories/CarRepository.ts
class CarRepository extends Repository<Car> {
  async findAvailableCars(): Promise<Car[]> {
    // Логіка пошуку доступних автомобілів
  }
}
```

#### 4. Services (Business Logic)
```typescript
// services/RentalService.ts
class RentalService {
  constructor(
    private rentalRepository: RentalRepository,
    private pricingStrategy: PricingStrategy
  ) {}
  
  async createRental(data: CreateRentalDto): Promise<Rental> {
    // Бізнес-логіка створення угоди
  }
}
```

#### 5. Controllers (REST API)
```typescript
// controllers/RentalController.ts
@Controller('/api/rentals')
class RentalController {
  @Post()
  async create(@Body() data: CreateRentalDto) {
    // Обробка HTTP запиту
  }
}
```

#### 6. Patterns (GoF Patterns)
```typescript
// patterns/strategy/PricingStrategy.ts
interface PricingStrategy {
  calculatePrice(car: Car, days: number): number;
}

class YearBasedPriceStrategy implements PricingStrategy {
  calculatePrice(car: Car, days: number): number {
    // Стратегія на основі року випуску
  }
}
```

### Frontend структура

#### 1. Components
```
components/
├── common/          # Загальні компоненти (Button, Input, Modal)
├── layout/          # Layout компоненти (Header, Footer, Sidebar)
├── car/             # Компоненти для автомобілів
├── client/          # Компоненти для клієнтів
└── rental/          # Компоненти для угод
```

#### 2. Pages
```
pages/
├── client/          # Клієнтська частина
│   ├── HomePage.tsx
│   ├── CarCatalog.tsx
│   └── MyRentals.tsx
└── admin/           # Адмін-панель
    ├── Dashboard.tsx
    ├── CarManagement.tsx
    └── Reports.tsx
```

#### 3. Services (API Clients)
```typescript
// services/api/carService.ts
class CarService {
  async getAllCars(): Promise<Car[]> {
    return axios.get('/api/cars');
  }
}
```

#### 4. Hooks (Custom Hooks)
```typescript
// hooks/useCars.ts
function useCars() {
  const [cars, setCars] = useState<Car[]>([]);
  // Логіка завантаження автомобілів
  return { cars, loading, error };
}
```

---

## 📦 Залежності проекту

### Backend (package.json)
```json
{
  "dependencies": {
    "express": "^4.18.2",
    "typeorm": "^0.3.17",
    "pg": "^8.11.0",
    "reflect-metadata": "^0.1.13",
    "class-validator": "^0.14.0",
    "class-transformer": "^0.5.1",
    "jsonwebtoken": "^9.0.2",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1",
    "nodemon": "^3.0.1",
    "@typescript-eslint/eslint-plugin": "^6.2.1",
    "eslint": "^8.45.0"
  }
}
```

### Frontend (package.json)
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.15.0",
    "axios": "^1.5.0",
    "@mui/material": "^5.14.5",
    "@mui/icons-material": "^5.14.5",
    "recharts": "^2.8.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.21",
    "@types/react-dom": "^18.2.7",
    "typescript": "^5.1.6",
    "vite": "^4.4.5",
    "@vitejs/plugin-react": "^4.0.3"
  }
}
```

---

## 🔐 Безпека

### Backend
- **JWT токени** для аутентифікації
- **bcrypt** для хешування паролів
- **class-validator** для валідації даних
- **CORS** налаштування
- **Helmet** для захисту заголовків (опціонально)

### Frontend
- Зберігання токенів в localStorage або httpOnly cookies
- Захист роутів (PrivateRoute компонент)
- Валідація форм на клієнті

---

## 📊 База даних - ER діаграма

```
┌─────────────┐         ┌─────────────┐
│   Client    │         │     Car     │
├─────────────┤         ├─────────────┤
│ id (PK)     │         │ id (PK)     │
│ full_name   │         │ brand       │
│ address     │◄────┐   │ model       │
│ phone       │     │   │ year        │
│ reg_date    │     │   │ type        │
└─────────────┘     │   │ price_per_day│
                    │   │ deposit      │
                    │   │ status       │
                    │   └─────────────┘
                    │          │
                    │          │
                    │          ▼
            ┌───────┴──────────────┐
            │      Rental          │
            ├──────────────────────┤
            │ id (PK)              │
            │ client_id (FK)       │
            │ car_id (FK)          │
            │ start_date           │
            │ expected_end_date    │
            │ actual_end_date      │
            │ deposit_amount       │
            │ total_cost           │
            │ penalty_amount       │
            │ status               │
            └──────────────────────┘
                    │
                    │
                    ▼
            ┌─────────────┐
            │   Penalty   │
            ├─────────────┤
            │ id (PK)     │
            │ rental_id(FK)│
            │ amount      │
            │ reason      │
            │ date        │
            └─────────────┘
```

---

## 🚀 Розгортання

### Локальне розгортання

1. **Встановити залежності:**
   ```bash
   # Backend
   cd backend
   npm install
   
   # Frontend
   cd frontend
   npm install
   ```

2. **Налаштувати базу даних:**
   ```bash
   # Створити БД
   createdb car_rental_db
   
   # Запустити міграції
   npm run migration:run
   ```

3. **Налаштувати змінні оточення:**
   ```bash
   # backend/.env
   DB_HOST=localhost
   DB_PORT=5432
   DB_USERNAME=postgres
   DB_PASSWORD=password
   DB_DATABASE=car_rental_db
   JWT_SECRET=your-secret-key
   ```

4. **Запустити сервери:**
   ```bash
   # Backend (термінал 1)
   cd backend
   npm run dev
   
   # Frontend (термінал 2)
   cd frontend
   npm start
   ```

### Docker розгортання (опціонально)

```yaml
# docker-compose.yml
version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: car_rental_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
  
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    depends_on:
      - postgres
  
  frontend:
    build: ./frontend
    ports:
      - "3001:3001"
    depends_on:
      - backend
```

---

## 📈 Масштабування та оптимізація

### Можливі покращення:
1. **Кешування** (Redis) для часто використовуваних даних
2. **Пагінація** для великих списків
3. **Індекси БД** для оптимізації запитів
4. **CDN** для статичних файлів
5. **Load balancing** для високого навантаження

---

## ✅ Переваги обраного стеку

1. **Швидкий розвиток**: TypeScript + готові бібліотеки
2. **Масштабованість**: Можна легко додавати нові функції
3. **Підтримка**: Велика спільнота та документація
4. **Сучасність**: Актуальні технології
5. **Гнучкість**: Легко інтегрувати нові сервіси

---

**Цей стек забезпечить якісну реалізацію всіх вимог проекту! 🎯**

