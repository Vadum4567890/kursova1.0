# Інструкції для створення репозиторію на GitHub

## Крок 1: Створення репозиторію на GitHub

1. Увійдіть на [GitHub](https://github.com)
2. Натисніть кнопку **"+"** в правому верхньому куті
3. Виберіть **"New repository"**
4. Заповніть форму:
   - **Repository name**: `kursova1.0` (або інша назва)
   - **Description**: "Система прокату автомобілів - курсовий проект"
   - **Visibility**: Public або Private (на ваш вибір)
   - **НЕ** створюйте README, .gitignore або LICENSE (вони вже є в проекті)
5. Натисніть **"Create repository"**

## Крок 2: Підключення локального репозиторію до GitHub

Відкрийте термінал в кореневій папці проекту та виконайте:

```bash
# Додайте remote репозиторій (замініть YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/kursova1.0.git

# Перевірте, що remote додано
git remote -v

# Змініть назву гілки на main (якщо потрібно)
git branch -M main

# Завантажте код на GitHub
git push -u origin main
```

## Крок 3: Перевірка

Перейдіть на сторінку вашого репозиторію на GitHub і переконайтеся, що всі файли завантажені.

## Крок 4: Налаштування GitHub (опціонально)

### Додавання опису репозиторію

На сторінці репозиторію натисніть ⚙️ (Settings) → в розділі "About" додайте:
- **Description**: "Система прокату автомобілів - курсовий проект"
- **Topics**: `car-rental`, `typescript`, `react`, `nodejs`, `express`, `course-project`

### Налаштування GitHub Pages (якщо потрібно)

1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main` / `docs`
4. Save

## Крок 5: Створення Issues та Projects (опціонально)

Можете створити Issues для відстеження завдань та Projects для планування роботи.

## Важливі файли, які вже включені:

✅ `.gitignore` - ігнорує node_modules, .env та інші файли
✅ `README.md` - опис проекту
✅ `DEPLOYMENT.md` - інструкції з розгортання
✅ `PROJECT_PLAN.md` - детальний план реалізації
✅ `TECHNOLOGY_STACK.md` - опис технологій
✅ `DESIGN_PATTERNS.md` - опис GoF-патернів

## Рекомендації для комітів:

Використовуйте описові повідомлення комітів:

```bash
git commit -m "feat: додано модель Car"
git commit -m "fix: виправлено помилку в розрахунку ціни"
git commit -m "docs: оновлено документацію"
git commit -m "refactor: рефакторинг RentalService"
```

Префікси:
- `feat:` - нова функціональність
- `fix:` - виправлення помилок
- `docs:` - зміни в документації
- `style:` - форматування коду
- `refactor:` - рефакторинг
- `test:` - додавання тестів
- `chore:` - зміни в конфігурації

## Наступні кроки:

1. Встановіть залежності: `npm install` в backend та frontend
2. Налаштуйте базу даних (див. DEPLOYMENT.md)
3. Запустіть проект
4. Продовжуйте розробку згідно з PROJECT_PLAN.md

---

**Успіхів з GitHub! 🚀**

