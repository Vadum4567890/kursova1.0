# 📸 Налаштування зображень автомобілів

## ✅ Що зроблено:

1. **Створено скрипт завантаження** (`backend/scripts/download-car-images.ts`)
   - Завантажує зображення з Unsplash
   - Зберігає їх локально в `backend/uploads/images/`

2. **Оновлено seed дані** (`backend/src/database/seed.ts`)
   - Всі автомобілі мають шляхи до локальних зображень
   - Формат: `/uploads/images/filename.jpg`

3. **Налаштовано статичний сервер** (`backend/src/index.ts`)
   - Зображення доступні через `/uploads/images/...`

4. **Оновлено фронтенд** (`frontend/src/pages/CarsPage.tsx`, `SearchPage.tsx`)
   - Правильне відображення локальних зображень
   - Fallback на placeholder при помилках

## 🚀 Як використовувати:

### 1. Завантажити зображення:

```bash
cd backend
npm run download-images
```

Це завантажить всі зображення в `backend/uploads/images/`:
- `toyota-corolla.jpg`
- `hyundai-elantra.jpg`
- `kia-rio.jpg`
- `bmw-3-series.jpg`
- `mercedes-c-class.jpg`
- `audi-a4.jpg`
- `bmw-x5.jpg`
- `mercedes-s-class.jpg`
- `porsche-cayenne.jpg`
- `toyota-camry.jpg`

### 2. Оновити базу даних:

```bash
cd backend
npm run seed
```

Це оновить всі записи автомобілів з правильними шляхами до зображень.

### 3. Перезапустити сервер:

```bash
cd backend
npm run dev
```

Зображення будуть доступні за адресою:
- `http://localhost:3000/uploads/images/toyota-corolla.jpg`
- `http://localhost:3000/uploads/images/bmw-3-series.jpg`
- і т.д.

## 📝 Додавання нових зображень:

1. Додайте новий URL в `backend/scripts/download-car-images.ts`
2. Запустіть `npm run download-images`
3. Оновіть seed.ts з новим шляхом
4. Перезапустіть seed скрипт

## 🔧 Альтернатива - завантаження через адмін-панель:

Можна також завантажувати зображення через API:
- `POST /api/upload/image` (потрібна авторизація admin/manager)
- Отриманий URL можна використовувати в `imageUrl` поля автомобіля

