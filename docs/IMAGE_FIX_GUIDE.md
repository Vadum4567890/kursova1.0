# 🔧 Виправлення зображень автомобілів

## Проблема
Деякі зображення не відповідають назвам автомобілів, оскільки в скрипті `download-car-images.ts` використовуються однакові URL для різних моделей.

## Поточний стан
Наразі деякі моделі використовують однакові URL:
- **Kia Rio** та **Audi A4** використовують той самий URL що і **Hyundai Elantra** (`photo-1606664515524`)
- **BMW X5** та **Porsche Cayenne** використовують той самий URL (`photo-1552519507`)
- **Toyota Corolla** та **Toyota Camry** використовують той самий URL (`photo-1605559424843`)

## Рішення

### Варіант 1: Ручне завантаження (Рекомендовано)
1. Відкрийте [Unsplash.com](https://unsplash.com) та знайдіть правильні зображення для кожної моделі:
   - Toyota Corolla
   - Hyundai Elantra
   - Kia Rio
   - BMW 3 Series
   - Mercedes-Benz C-Class
   - Audi A4
   - BMW X5
   - Mercedes-Benz S-Class
   - Porsche Cayenne
   - Toyota Camry

2. Для кожного зображення:
   - Натисніть на зображення
   - Скопіюйте URL зображення (права кнопка миші → "Copy image address")
   - Або скопіюйте photo ID з URL (наприклад, `photo-1605559424843`)

3. Оновіть `backend/scripts/download-car-images.ts` з правильними URL:
   ```typescript
   {
     filename: 'kia-rio.jpg',
     url: 'https://images.unsplash.com/photo-XXXXXXXXXXXX?w=800&h=600&fit=crop&q=80',
     car: 'Kia Rio'
   },
   ```

4. Запустіть скрипт:
   ```bash
   cd backend
   npm run download-images
   ```

5. Перезапустіть backend сервер

### Варіант 2: Використання Pexels API (Альтернатива)
Pexels має кращу категорізацію автомобілів. Можна використати їх API для пошуку правильних зображень.

### Варіант 3: Локальні зображення
1. Завантажте зображення вручну з будь-якого джерела
2. Збережіть їх в `backend/uploads/images/` з правильними назвами:
   - `toyota-corolla.jpg`
   - `hyundai-elantra.jpg`
   - `kia-rio.jpg`
   - `bmw-3-series.jpg`
   - `mercedes-c-class.jpg`
   - `audi-a4.jpg`
   - `bmw-x5.jpg`
   - `mercedes-s-class.jpg`
   - `porsche-cayenne.jpg`
   - `toyota-camry.jpg`

3. Переконайтеся, що назви файлів відповідають тим, що вказані в `seed.ts`

## Швидке виправлення
Якщо потрібно швидко виправити, можна:
1. Використати placeholder зображення для тих моделей, які не мають унікальних фото
2. Або використати різні Unsplash photo IDs для кожної моделі (знайти на unsplash.com)

## Примітка
Для курсового проекту можна використати будь-які зображення автомобілів, головне щоб вони відповідали класу (економ, бізнес, преміум) та виглядали реалістично.
