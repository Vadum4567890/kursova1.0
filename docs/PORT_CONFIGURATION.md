# 🔌 Конфігурація портів

## Поточна конфігурація

### Backend (API Server)
- **Порт**: `3000`
- **URL**: `http://localhost:3000`
- **Swagger**: `http://localhost:3000/api-docs`
- **API Base**: `http://localhost:3000/api`

### Frontend (React + Vite)
- **Порт**: `3001`
- **URL**: `http://localhost:3001`
- **Proxy**: Автоматично проксує `/api/*` на `http://localhost:3000/api`

## Як це працює

1. **Backend** запускається на порту **3000**
2. **Frontend** запускається на порту **3001**
3. Vite автоматично проксує всі запити `/api/*` на backend

### Приклад:
- Frontend запит: `http://localhost:3001/api/cars`
- Vite проксує на: `http://localhost:3000/api/cars`

## Запуск проекту

### 1. Запустити Backend:
```bash
cd backend
npm run dev
```
Backend буде доступний на `http://localhost:3000`

### 2. Запустити Frontend (в новому терміналі):
```bash
cd frontend
npm run dev
```
Frontend буде доступний на `http://localhost:3001`

## Зміна портів

### Backend
Створіть файл `backend/.env`:
```env
PORT=3000
```

### Frontend
Змініть `frontend/vite.config.ts`:
```typescript
server: {
  port: 3001, // Змініть на потрібний порт
  proxy: {
    '/api': {
      target: 'http://localhost:3000', // Змініть якщо backend на іншому порті
      changeOrigin: true,
    },
  },
}
```

## Проблема: "evmAsk.js:5 Uncaught TypeError"

Ця помилка **НЕ пов'язана з нашим проектом**. Це помилка від браузерного розширення (найчастіше MetaMask або інший Web3 wallet).

### Рішення:
1. **Вимкнути розширення** в браузері (MetaMask, Trust Wallet, тощо)
2. Або **ігнорувати помилку** - вона не впливає на роботу нашого додатку
3. Або використовувати **інший браузер** для розробки

### Як вимкнути розширення:
- **Chrome**: `chrome://extensions/` → знайдіть MetaMask → вимкніть
- **Firefox**: `about:addons` → знайдіть MetaMask → вимкніть
- **Edge**: `edge://extensions/` → знайдіть MetaMask → вимкніть

## Перевірка портів

### Перевірити які порти зайняті:
**Windows (PowerShell):**
```powershell
netstat -ano | findstr :3000
netstat -ano | findstr :3001
```

**Linux/Mac:**
```bash
lsof -i :3000
lsof -i :3001
```

### Звільнити порт (якщо зайнятий):
**Windows:**
```powershell
# Знайти процес
netstat -ano | findstr :3000
# Вбити процес (замініть PID на номер процесу)
taskkill /PID <PID> /F
```

**Linux/Mac:**
```bash
# Знайти процес
lsof -ti:3000
# Вбити процес
kill -9 $(lsof -ti:3000)
```

## Troubleshooting

### Проблема: "Port already in use"
**Рішення**: Backend автоматично спробує наступний порт (3001, 3002, тощо), але краще звільнити потрібний порт.

### Проблема: "Failed to fetch" в Swagger
**Рішення**: Перевірте що:
1. Backend запущений на порту 3000
2. CORS налаштований правильно
3. Немає конфліктів з портами

### Проблема: Frontend не може підключитися до API
**Рішення**: Перевірте що:
1. Backend запущений
2. `vite.config.ts` має правильний proxy
3. `frontend/src/services/api.ts` має правильний `API_URL`

