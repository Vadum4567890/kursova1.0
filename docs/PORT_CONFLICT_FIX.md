# 🔧 Вирішення конфлікту портів

## Проблема
Коли порт 3000 зайнятий, backend сервер автоматично переключається на порт 3001, що конфліктує з фронтендом.

## Рішення

### Варіант 1: Використання скрипта kill-port (Рекомендовано)

1. **Завершити процес на порту 3000:**
   ```bash
   cd backend
   npm run kill-port
   ```

2. **Або вказати інший порт:**
   ```bash
   npm run kill-port 3000
   ```

3. **Після цього запустити сервер:**
   ```bash
   npm run dev
   ```

### Варіант 2: Ручне завершення процесу (Windows)

1. **Знайти процес на порту 3000:**
   ```bash
   netstat -ano | findstr :3000
   ```

2. **Завершити процес (замініть <PID> на номер процесу):**
   ```bash
   taskkill /PID <PID> /F
   ```

3. **Або завершити всі Node.js процеси:**
   ```bash
   taskkill /IM node.exe /F
   ```

### Варіант 3: Використання Task Manager

1. Відкрити **Task Manager** (Ctrl + Shift + Esc)
2. Перейти на вкладку **Details**
3. Знайти процеси `node.exe` або `ts-node.exe`
4. Завершити їх

## Зміни в коді

Тепер backend сервер **НЕ** буде автоматично переключатися на інший порт. Якщо порт 3000 зайнятий, сервер виведе помилку і завершить роботу з інструкціями.

## Нові npm скрипти

- `npm run kill-port` - завершити процес на порту 3000 (або вказати інший порт)
- `npm run dev` - запустити сервер (якщо порт зайнятий, буде помилка)

## Примітка

Якщо ви хочете, щоб сервер автоматично завершував старий процес перед запуском, використовуйте:
```bash
npm run kill-port
npm run dev
```

Або створіть власний скрипт для автоматизації цього процесу.

